apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.metadata.name }}
  labels:
    app: memcached
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.service.spec.port.number }}
      targetPort: {{ .Values.service.spec.port.number }}
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: memcached